# MDowlet runtime dependencies
# Install with: pip install -r requirements.txt

PySide6>=6.6.0
PySide6-WebEngine>=6.6.0
platformdirs>=4.0.0

# Dev / build only (not required at runtime)
# pyinstaller>=6.0.0
# dmgbuild>=1.6.0   (macOS only, optional)
